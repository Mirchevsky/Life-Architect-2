package com.mirchevsky.lifearchitect2.data

/**
 * Represents the user's theme preference.
 * SYSTEM follows the Android device setting (default on first launch).
 */
enum class Theme {
    LIGHT, DARK, SYSTEM
}
